import Builds from "@/components/sections/Builds";

export const metadata = {
  title: "Lab — Andrei Foitoș",
};

export default function BuildsPage() {
  return (
    <div className="page-wrap">
      <Builds />
    </div>
  );
}